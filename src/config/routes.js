const routes = {
  login: '/',
  home: '/home',
  forbidden: '/forbidden',
  internalServer: '/server-error',
  notFound: '*',
  dashboard: '/dashboards',
  users: '/users',
  account: 'account',
};

export default routes;
