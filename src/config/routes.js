const routes = {
  login: '/',
  home: '/home',
  forbidden: '/forbidden',
  internalServer: '/server-error',
  notFound: '*',
  dashboard: '/dashboards',
  users: '/users',
};

export default routes;
